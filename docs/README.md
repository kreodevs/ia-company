# docs/

Agent and platform documentation outputs.

| Document | Description |
|----------|-------------|
| [`platform.md`](platform.md) | Auto-Company v2 multi-tenant platform — architecture, consensus cycle, deploy |

Role-specific agent outputs live in subfolders (`ceo/`, `cto/`, etc.) per `CLAUDE.md`.
