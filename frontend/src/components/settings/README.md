# Settings components

| Component | Purpose |
|-----------|---------|
| `OrchestrationPlanPanel.tsx` | Unified tenant orchestration plan: presets, rules, cron/interval timing, conditions. Dynamic mode shows an info panel explaining that no fixed workflow is selected because the native meta-orchestrator picks one each cycle. |

Used by `SettingsPage` (Schedules tab).
