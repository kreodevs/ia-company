# Settings components

| Component | Purpose |
|-----------|---------|
| `OrchestrationPlanPanel.tsx` | Unified tenant orchestration plan: presets, rules, cron/interval timing, conditions |

Used by `SettingsPage` (Schedules tab).
