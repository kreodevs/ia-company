# Local development override — publish web on host port (default 8080).
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml --env-file .env.production up -d --build

services:
  web:
    ports:
      - "${WEB_PORT:-8080}:80"
